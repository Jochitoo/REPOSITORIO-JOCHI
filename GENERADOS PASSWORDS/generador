import random
import string
todo=string.ascii_letters + string.digits + string.punctuation
bucle=True
while bucle:
    longitud=int(input("12 caracteres "))
    password="".join(random.sample(todo,longitud))
    print("Tu contraseña) es: ",password)
    bucle=False